from tkinter import *
class window:
    def __init__(self):
        search = Tk()
        search.title("Search for a movie")
        search.minsize(500, 500)
        search.maxsize(500, 500)
        search.mainloop()